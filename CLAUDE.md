<div dir="rtl" style="text-align: right;">

# أداة تشييك تغليف الأوردرات — Pack Checker (`Orders-Packing-Checker`)

![version](https://img.shields.io/badge/version-v2.6.2-blue)

**بتعمل إيه:** بتعرض للموظف **طابور الأوردرات الجاهزة للتغليف** تحت مربعات
الإدخال، وبيسكن باركود الأوردر (أو **تراكينج بوسطة**، أو بيدخّل الرقم بإيده)،
الأداة بتجيب منتجاته من Shopify، بيشيّك عليها قطعة قطعة، وبتسجّل التغليف على
الأوردر (ميتافيلد + tag + D1).
**مين بيستخدمها:** المخزن (التغليف)
**الإصدار:** Worker `v2.4.0` · الواجهة `v2.6.2` · `MIN_WORKER_VERSION = 2.4.0`   ← الاتنين مستقلين، طبيعي يختلفوا

> 🔗 **من v2.2.0 الأداة دي بقت مدموجة مع «أوردرات جاهزة للتغليف»** (`Ready-to-Pack`).
> التفاصيل في قسم «الدمج» تحت — **اقراه قبل أي تعديل على الطابور أو على تصنيف S2.**

## الروابط

```
الواجهة    : https://ecommoda-dev.github.io/Orders-Packing-Checker/
الـ Worker : https://orders-packing-checker-worker.ecommoda-dev.workers.dev
اسم الـ Worker في الداشبورد: orders-packing-checker-worker   ← مطابق لـ name في wrangler.toml
```

## الـ Endpoints

| `?action=` | بيعمل إيه |
|---|---|
| `get_order` | يجيب الأوردر (بالـ `id` أو الـ `name`، **أو `tracking` من v2.4.0** — تراكينج بوسطة بيتحوّل لاسم أوردر الأول)، يحدّد المرحلة S1/S2، ويصنّف المنتجات. بيرجّع `truncated` لو الأوردر أكبر من اللي اتجاب، و`order.bosta` لو الدخول كان من تراكينج |
| `complete_pack` | **بيقرا الأوردر من شوبيفاي الأول** (v2.1.0)، يحسب المرحلة بنفسه، يتحقق من حارس «اتغلّف قبل كده»، وبعدين يكتب `s1/s2_packed_by` + `s1/s2_packing_date_time` + tag `S1=Packed`/`S2=Packed` ويسجّل في D1. بيرجّع `status` (`success`/`warning`/`error`) + `actions[]` + `logged`، أو **`409` + `conflict: true`** لو الأوردر اتغلّف بالفعل |
| `get_ready_orders` | **(v2.2.0)** طابور «جاهز للتغليف»: أوردرات S1 + S2 حالتها `Ready` واتطبعت ولسه ما اتغلّفتش، مرتّبة بتاريخ الإنشاء تنازليًا. **قراءة بحتة** — مفيش أي ميوتيشن ولا كتابة D1. بيرجّع `partial` + `s2Failed` + `s2Truncated`. **من v2.3.0** بيرجّع كمان `zone` من `custom.zone` |
| `diag` | فحص ذاتي بدون أي كتابة (متغيّرات · Origin · D1 · صلاحيات تطبيق شوبيفاي) — **مابيرجّعش قيمة أي سر** |
| `get_config` | `WORKER_VERSION` — الواجهة بتقارنه بـ `MIN_WORKER_VERSION` |
| `check_employee` · `register_pin` · `verify_employee` · `log_logout` · `get_employees` | البلوك المشترك للدخول |
| `get_logs` · `get_logs_count` · `get_logs_export` | سجل العمليات — فلترة server-side بقوايم + `dateFrom`/`dateTo` |

## D1

```
tool  : pack_checker
type  : packed · login · logout
```

**أعمدة إضافية في `logs` بتتكتب من الأداة دي** (مش جداول زيادة — نفس جدول
`logs` المشترك): `stage` · `item_count` · `items` · `edit_reason`.
بتتكتب في `UPDATE` منفصل **بعد** `writeLog` مباشرةً، والمطابقة بتحصل بـ
`(tool, type, order_name, timestamp)`.

> **من v2.0.0 الـ `UPDATE` ده بقى متحقَّق منه** (`meta.changes > 0`) — لو ما طابقش
> صف، الرد بيرجع `columnsWritten:false` و`status:'warning'` والواجهة بتعرض تحذير.
> وكمان: **كشف التغيير ماعادش معتمد على الأعمدة دي** — البصمة نفسها اتخزّنت في
> `extra.fingerprint` جوّه الـ `INSERT` الأصلي، فحتى لو الـ `UPDATE` فشل الكشف
> بيفضل شغّال.

`extra` بيتخزّن فيه: `stage` · `packedBy` · `packingDate` · `itemCount` · `items`
· `fingerprint` · `editReason` · **`result`** (`success`/`warning`) · `actions` ·
`warnings`. عمود «النتيجة» في تاب السجل بيقرا `extra.result`، والصفوف القديمة
اللي مفيهاش الحقل ده بتتعرض **"—" مش "✓"** — إحنا فعليًا مش عارفين.

> `edit_count` لسه في الـ schema للتوافق بس — **مابيتكتبش** من v1.3.0+.

## المضبوط فعليًا في الداشبورد

> اللي **متظبط بالفعل** — مش اللي المفروض يكون.

```
Bindings : DB → ecommoda-dev-logs
Secrets  : WORKER_SECRET · CLIENT_ID · CLIENT_SECRET · BOSTA_API_KEY   ← الأخير جديد في v2.4.0
Vars     : SHOP_DOMAIN                       ← من [vars] في wrangler.toml
Build watch paths : * (الافتراضي) — لسه ما اتضيّقتش (راجع «مسائل مفتوحة»)
compatibility_date: 2026-09-03  ← اتحرّك 03-09-2026 بعد فحص الأعلام (R12)
```

**تصنيف الـ `env.*` (إجراء §4-أ-٢ في `ecommoda-tool-migration-playbook`):**

| النوع | المتغيّرات |
|---|---|
| **سر** (قيمته مستحيلة القراءة) | `WORKER_SECRET` · `CLIENT_ID` · `CLIENT_SECRET` · `BOSTA_API_KEY` |
| **var بيفشل بصوت عالي لو غاب** | `SHOP_DOMAIN` |
| **var ليه fallback (الخطر الصامت)** | ✅ **لا شيء** — مفيش أي `env.X \|\| ...` في الكود |

> ومن v2.0.0 فيه `assertEnv(env, 'shopify')` قبل أي نداء شوبيفاي: أي متغيّر ناقص
> بيوقف العملية **برسالة باسمه**، و`?action=diag` بيعرض الأسماء والأطوال (مش القيم).

## CORS

`ALLOWED_ORIGINS` **صارمة** (مش wildcard) — لأن الأداة **أداة كتابة**
(بتكتب ميتافيلدات وtags على أوردرات حية).

```
https://ecommoda-dev.github.io   ← الوحيد
```

> الدومين المهجور `https://ecommoda24.github.io` **اتشال في v2.0.0**. كان
> `ALLOWED_ORIGINS[0]` يعني **قيمة الـ fallback** في `getCORS()`، فأي origin غير
> معروف كان بياخد ترويسة CORS بدومين مهجور. (`ecommoda-constants` §5 و§11 بند 10.)

## خط الأساس قبل النقل

> مستنتج من D1 (ما اتداش يدويًا) — قراءة يوم 03-09-2026:

```
tool = 'pack_checker'
  packed : 7,534 صف   (18-05-2026 → 02-09-2026)
  login  :   277 صف   (17-05-2026 → 02-09-2026)
  logout :     0 صف
```

> **سبب الصفر اتحدّد في v2.0.0:** مكانش فيه **زرار خروج في الواجهة خالص** —
> `log_logout` موجود في الـ Worker من الأول وما كانش بيتنادى. الزرار اتضاف
> (`doLogout()` في `§AUTH-JS`)، فالمتوقع الصف ده يبدأ يتملّي من دلوقتي.

استعلام المقارنة بعد النقل (سطر واحد للّصق في D1 Console):

```sql
SELECT type, COUNT(*) AS n, MAX(timestamp) AS last_ts FROM logs WHERE tool = 'pack_checker' GROUP BY type;
```

## `compatibility_date` — اتحرّكت 03-09-2026 (R12)

```
2025-01-01  →  2026-09-03
```

**التاريخ ده بيعمل إيه:** بيقول لكلاودفلير «الكود ده اتكتب على السلوك اللي كان
موجود في اليوم ده». أي إصلاح **بيغيّر سلوك** ونزل بعد التاريخ ده مابيوصلش
للأداة إلا لما التاريخ يتحرّك بإيدك — عشان إصلاح في الـ runtime ما يكسرش أداة
شغّالة من غير ما حد يعمل deploy.

⚠️ **مش نسخة الـ runtime.** الـ Worker شغّال على أحدث runtime دايمًا مهما كان
التاريخ، وباتشات الأمان بتوصل عادي. المتجمّد هو **السلوك** بس.

**الفحص اللي اتعمل قبل التحريك** — كل سلوك اتغيّر افتراضيًا في الفترة دي
اتقارن بالكود الفعلي للأداة دي، مش بالتخمين ومش نقلًا عن أداة تانية:

| العَلَم | من | الأثر المحتمل | على الأداة دي |
|---|---|---|---|
| `fetch_iterable_type_support` | 2026-02-19 | **كاسر** — `Array` كـ body مابقاش يتحوّل لنص | ✅ آمن — كل `body:` في الملف `JSON.stringify(...)` (نص) |
| `strip_authorization_on_cross_origin_redirect` | 2025-09-01 | ترويسة `Authorization` بتتشال عند redirect لأصل تاني | ✅ آمن — مفيش `Authorization` في أي نداء **صادر**؛ شوبيفاي بتاخد `X-Shopify-Access-Token`. الترويسة دي بتتقرا من الطلب **الوارد** بس |
| `enable_nodejs_global_timers` | 2026-02-10 | `setTimeout` بترجّع `Timeout` مش رقم | ✅ آمن — كل النداءات جوّه `new Promise(r => setTimeout(r, …))` ومفيش استخدام لقيمة الإرجاع |
| `nodejs_compat` + `_v2` | 2026-08-04 | بيتفعّلوا افتراضيًا ويضيفوا globals (`process` · `Buffer`…) | ✅ آمن — صفر تعارض أسماء في الملف |
| `urlpattern_standard` · `strip_bom_in_read_all_text` · `text_decoder_replace_surrogates` · `require_returns_default_export` · `rpc_params_dup_stubs` | 2025-05 → 2026-02 | — | ✅ غير منطبقة — `URLPattern` · `readAllText` · `TextDecoder` · `require()` · RPC كلها **صفر استخدام** |
| باقي أعلام `node:*` | 2025-09 → 2026-03 | وحدات Node إضافية | ✅ غير منطبقة — الأداة مش بتستخدم Node |

**مفيش `compatibility_flags`** — الافتراضي مظبوط، ومفيش داعي لأي استثناء.

⚠️ **لو ظهر أي سلوك غريب بعد النشر**، الرجوع خطوة واحدة: رجّع التاريخ لـ
`2025-01-01` وانشر. الأعلام كلها ليها نسخة `disable_*` كمان لو الحاجة لواحد بعينه.


## الدمج مع «أوردرات جاهزة للتغليف» — v2.2.0 (05-09-2026)

أداة `Ready-to-Pack` كانت **واجهة عرض** بتقول للمخزن «دي الأوردرات اللي
قدامك دلوقتي»، والأداة دي بتغلّفها. الموظف كان لازم يفتح تبويبتين ويقارن
بعينه. من v2.2.0 الطابور جوّه شاشة التغليف نفسها.

### شكل الدمج — القرارات (أحمد، 05-09-2026)

| القرار | التنفيذ |
|---|---|
| **مش تاب منفصلة** | الطابور جوّه `#subscreenEntry` في تاب «🔍 فحص أوردر»، **تحت** مربعات السكانر والإدخال اليدوي مباشرةً |
| **الضغط على صف مابيفتحش التشييك** | الجدول **عرض بس**. مفيش `onclick` على أي `<tr>`. الدخول للتغليف من السكانر أو الإدخال اليدوي زي ما هو |
| **ريبو وWorker الـ Ready-to-Pack زي ما هما** | ما اتلمسوش. الأداتين شغّالين بالتوازي دلوقتي — راجع «الخطر المفتوح» تحت |
| **اسم الأداة في D1 ما اتغيّرش** | `pack_checker` زي ما هو. `get_ready_orders` **مابيكتبش في D1 أصلاً**، فمفيش `type` جديد ومفيش تسجيل مطلوب في `ecommoda-constants` §7 (Rule 7 مش منطبقة هنا) |

رقم الأوردر في الجدول **لينك لشوبيفاي** (قاعدة #20) — ده لينك خارجي، مش
فتح لشاشة التشييك. مفيش تعارض مع القرار فوق.

### إمتى الطابور بيتحدّث

> ⚠️ **اتغيّر في v2.4.0** — بقى فيه تحديث تلقائي كل ١٥ دقيقة كمان.
> السطر «مفيش polling» اللي كان هنا **مابقاش صحيح**. راجع قسم v2.4.0.

اللحظات دي:

1. **بعد الدخول** — `finishLogin()` بينادي `rdyLoadOrders()` بدون `await`،
   عشان مايأخّرش فتح الشاشة ومايخدش الفوكس من مربع السكانر.
2. **زرار «تحديث»** — الرئيسي في صندوق الأعداد، والتاني فوق الجدول.
3. **بعد أي تغليف ناجح أو جزئي** — الأوردر اتكتب عليه فعلاً في الحالتين،
   يعني المفروض يخرج من الطابور. **قايمة كاذبة أسوأ من قايمة قديمة.**
   ⚠️ **من v2.3.0 اللحظة دي بقت «عند الرجوع لشاشة الطابور»** مش لحظة
   الحفظ — `rdyDirty` + `rdyRefreshIfDirty()`. راجع قسم v2.3.0 تحت.

> `silent: true` معناها: مفيش حالة «جاري الجلب»، ومفيش توست على الفشل —
> لأن التغليف نفسه نجح وبيتعرض في نتيجته، وتحذير عن الطابور فوقه هيلخبط.
> التحديث اليدوي **مش** صامت وبيعرض الخطأ كامل.

### إيه اللي اتوحّد فعلاً — وده أهم جزء

الأداتين كانوا بيقروا **نفس الميتافيلدات بنفس المنطق من ملفين منفصلين**.
ده مش تكرار جمالي — ده اللي أنتج باج حقيقي:

> `refundedIds` اتشالت من `classifyOrderItems` هنا في **v1.5.0**، وفضلت
> مكسورة في `ready_to_pack` لحد **v2.1.0**. يعني لشهور، الأداتين كانوا
> بيقولوا **حاجتين متضادتين عن نفس الأوردر**: الطابور بيقول «استرجاع بس»
> ويخفيه، والتغليف بيفتحه ويطلّع البنود عادي.

من v2.2.0 `classifyS2Subtype` **نسخة واحدة** في `index.js` — ومعاها كل
حراس `ready_to_pack` v2.1.0 اتنقلوا حرفيًا:

- `isPrintedNotPacked` — «اتطبع **بعد** آخر تغليف؟» مش «متملّي ولا فاضي»
- شرط الكمية الحيّة على بنود الاستبدال الرسمية (R2-ب هناك)
- التصنيف على **أحدث دورة** بس، مرتّبة بالـ `createdAt` (R2-ج)
- `classifyS2Batch` بتزامن محدود + `s2Failed` بدل الابتلاع الصامت
- `s2Truncated` — «صنّفنا بس مش شايفين كل حاجة» ≠ `s2Failed`

### ⚠️ الخطر المفتوح — الأداتين لسه شغّالين بالتوازي

`ready-to-pack-worker` **لسه منشور وشغّال** (قرار: نسيبه زي ما هو).
يعني نفس المنطق دلوقتي في **مكانين**:

```
Orders-Packing-Checker/index.js  §READY::classifyS2Subtype   ← المستخدم في الأداة المدموجة
Ready-to-Pack/index.js           §SHOPIFY::classifyS2Subtype ← لسه شغّال لوحده
```

**ده بالظبط الوضع اللي أنتج باج R1.** أي إصلاح على تصنيف S2 أو على
`isPrintedNotPacked` لازم يتعمل في **الاتنين** لحد ما `Ready-to-Pack`
يتقفل — أو الأداتين هيرجعوا يقولوا حاجتين متضادتين.

**الخروج من الخطر ده:** لما يتأكد إن المخزن بيستخدم الأداة المدموجة بس،
`Ready-to-Pack/index.html` يتحوّل لصفحة تحويل و`ready-to-pack-worker`
يتقفل. القرار ده **مؤجّل مش ملغي**.


## طابور التغليف — تحديثات الواجهة v2.3.0 (05-09-2026)

**واجهة بس. `index.js` ما اتلمسش ومفيش Promote مطلوب** — كل اللي تحت
محسوب في المتصفح من نفس بيانات `get_ready_orders`.

### ترتيب أعمدة الطابور (قرار أحمد، 05-09-2026)

```
رقم الأوردر · المندوب · تاريخ الطباعة · وقت الطباعة ·
الوقت منذ الطباعة · تاريخ الأوردر · نوع الأوردر · المحاولة
```

التصدير XLSX **بنفس الترتيب** + عمود «عمر الأوردر» منفصل (في الجدول هو
بادج جوّه عمود تاريخ الأوردر، وفي ملف Excel عمود لوحده عشان يتفلتر).
**ملف تصدير بترتيب مخالف للجدول بيخلّي اللي بيراجعه يقارن عمود بعمود غلط.**

### العمودان المحسوبان — الوحدة **يوم تقويمي** مش ٢٤ ساعة

`rdySincePrint` و`rdyOrderAgeText` بيحسبوا بفرق **الأيام التقويمية بتوقيت
القاهرة** (`rdyCairoDayIndex` من `toCairo`)، مش `(الآن − الوقت)/86400000`.

> أوردر اتطبع ١١ مساءً بيبقى **«متأخر يوم»** الساعة ١ صباحًا — وده الصح
> للمخزن: «عدّى اليوم» معناها التاريخ اتغيّر، مش إن ٢٤ ساعة عدّت.
> والحساب من `toCairo` مش `iso.slice(0,10)` — التانية كانت هتقفل اليوم
> الساعة ٣ صباحًا بتوقيتنا (نفس فخّ `rdyCairoDateStr` الموجود من v2.2.0).

سلّم «الوقت منذ الطباعة» — **درجة واحدة لكل اليوم**، والدرجة بتتغيّر مع كل
يوم تقويمي جديد:

| الفرق | الكلاس | اللون | الرسالة |
|---|---|---|---|
| نفس اليوم | `since-d0` | أخضر | 🟢 منذ ٢٠ دقيقة · 🟢 منذ ٣ ساعات و١٠ دقايق |
| يوم | `since-d1` | كهرماني | 🟡 متأخر يوم — من امبارح |
| يومين | `since-d2` | برتقالي | 🟠 متأخر يومين |
| ٣–٦ | `since-d3` | أحمر | 🔴 متأخر N أيام |
| ٧ فأكتر | `since-d7` | أحمر داكن مصمت | ⛔ متأخر N يوم |

> ⚠️ **ألوان السلّم كلها متغيّرات في `:root`** (`--orange-dark` · `--crit*`
> اتضافوا في v2.3.0) — القاعدة «صفر `#hex` حرفي خارج `:root`» سارية.

### الترتيب الافتراضي — تاريخ الأوردر تصاعديًا

`rdyDefaultSort` بترتّب **الأقدم أولاً**، والضغطة التالتة على أي عمود
بترجّع لها مش لترتيب الـ Worker.

> الـ Worker لسه بيرجّع **الأحدث أولاً** — الترتيب الافتراضي بقى قرار
> واجهة عشان يتغيّر من غير deploy ولا Promote. لو حد غيّر ترتيب الـ Worker
> بعد كده، ده **مش** هيغيّر اللي الموظف بيشوفه.

عمود «الوقت منذ الطباعة» بيترتّب بنوع `elapsed` (اتجاه معكوس للتاريخ):
تصاعدي = **الأقل تأخيرًا الأول**، والأوردر اللي مالوش وقت طباعة بيروح آخر
القايمة في التصاعدي.

### زرار التحديث ولوحة «آخر تحديث»

الزرار الرئيسي والبادج **في هيدر الطابور مش جوّه صندوق العدّ** — الصندوق
بيختفي في حالتَي «في انتظار التحديث» و«لا توجد أوردرات»، وهما بالظبط
الحالتين اللي المستخدم محتاج فيهم زرار تحديث. (الزرار القديم في شريط
الجدول لسه مكانه، والاتنين بيتقفلوا مع بعض في `rdySetRefreshing`.)

`rdyRenderFreshness` بترسم الختم + «منذ كام»، والعدّاد بيلوّن نفسه:
`< ٥ د` أخضر · `< ١٥ د` أزرق · `< ٦٠ د` كهرماني (والزرار كمان بيبقى
كهرماني) · `≥ ساعة` أحمر «الطابور قديم، حدّث».

> ⚠️ **الختم بيتسجّل حتى لو الطابور رجع فاضي** — «اتحدّث ورجع صفر» معلومة،
> و«ما اتحدّثش» معلومة تانية خالص. وفي الفشل الختم **مابيتغيّرش** — عشان
> ما نقولش للموظف إن القايمة دي جديدة وهي مش جديدة.

### النبضة — `§READY-JS::ticker`

`setInterval` واحد كل ٣٠ ثانية بيحدّث: «منذ كام» + كل خلايا `data-rdy-since`
و`data-rdy-age` **في مكانها** (`textContent` + `className`).

- **مفيش أي نداء شبكة في النبضة** — دي أرقام محسوبة محليًا. **مفيش polling**
  على `get_ready_orders`، وما ينفعش يتضاف من غير قرار: الـ endpoint ده
  بيعمل تصفيح على كل الأوردرات + تصنيف S2 لكل أوردر.
- **ممنوع إعادة رسم الجدول في النبضة** — `innerHTML` بيصفّر تمرير الجدول
  تحت إيد الموظف كل نص دقيقة.
- النبضة بتوقف في `rdyResetAll` (الخروج) وبتبدأ بعد أول جلب ناجح.

### التحديث التلقائي بعد التغليف — بقى «عند الرجوع»

`completePack` بيعلّم `rdyDirty = true` بس؛ الجلب بيحصل في
`rdyRefreshIfDirty()` اللي بتتنادى من **`resetToEntry`** ومن
**`switchMainTab('scan')`**.

| قبل (v2.2.0) | بعد (v2.3.0) |
|---|---|
| جلب فوري في لحظة الحفظ | جلب عند الرجوع لشاشة الطابور |
| استعلام حتى لو الموظف فضل في شاشة التشييك | استعلام واحد، وقت ما القايمة هتتشاف فعلاً |
| الرجوع من تاب السجل مابيحدّثش | بيحدّث لو فيه تغليف حصل |

> ⚠️ `rdyDirty` **مابيتصفّرش غير بعد جلب ناجح** — لو الجلب فشل (صامت)،
> الفلاج بيفضل مرفوع وأول رجوع تاني بيحاول. و`rdyLoading` بيمنع نداءين
> متوازيين (رجوع + تبديل تاب في نفس اللحظة).

## طابور التغليف — v2.4.0 (05-09-2026)

> ⚠️ **النسخة دي فيها تعديل على الـ Worker** (`custom.zone` في
> `READY_LIST_QUERY` + `shapeReadyOrder`) — يعني **Promote مطلوب**.
> `MIN_WORKER_VERSION` بقى `2.3.0`، فمن غير Promote الواجهة هتطلّع تحذير
> «⚠️ الـ Worker نسخة قديمة» بدل ما عمود المنطقة يفضل فاضي بصمت.

### ترتيب الأعمدة (قرار أحمد، 05-09-2026)

```
خيارات · رقم الأوردر · المنطقة · المندوب · تاريخ الطباعة · وقت الطباعة ·
الوقت منذ الطباعة · تاريخ الأوردر · نوع الأوردر · المحاولة
```

### `custom.zone` — المنطقة

> ⚠️ **العمود ده اتشال نهائيًا من الواجهة في v2.6.2** (قرار أحمد
> 05-09-2026) — القسم ده بقى **تاريخي**: بيوصف اللي كان موجود وقت
> v2.4.0، مش اللي موجود دلوقتي. الـ Worker لسه بيرجّع القيمة، بس الواجهة
> مابتقراهاش. راجع قسم «v2.6.2» تحت لتفاصيل الحذف والبديل.

الميتافيلد choice list بأربع قيم، والترجمة في `RDY_ZONE_MAP`:

| القيمة | المعروض | اللون |
|---|---|---|
| `Cairo+Giza` | قاهرة وجيزة | أزرق |
| `Other_Regions` | محافظات | أحمر |
| `Show_Room` | شو روم | أخضر |
| `BLANK` **أو غايب تمامًا** | ⚠ لا توجد منطقة مسجلة ⚠ | برتقالي (تحذير) |
| أي قيمة تانية | القيمة **زي ما هي** | بادج محايد |

> ⚠️ **الغايب زي `BLANK`** — الاتنين تحذير مش خانة فاضية: منطقة ناقصة قرار
> شحن ناقص. وقيمة **مش في القايمة** بتتعرض زي ما هي — ما بنترجمش قيمة
> معرفناهاش وما بنخبّيهاش (تعريف الميتافيلد نفسه بيقول «فيه قيم غير صالحة»).

> ⚠️ **الترجمة بتحصل مرة واحدة وقت بناء الصف** (`zone` = النص المعروض،
> `zoneCls` = لونه). لو `rdyRenderTable` نادت `rdyZoneInfo` تاني، هتترجم نص
> عربي **مترجم أصلاً** والبادج هيفقد لونه.

### المندوب — القيمة زي ما هي (تغيير كاسر للعرض)

لحد v2.3.0: `Bosta` → «بوسطة» و**أي قيمة تانية** → «قاهرة». يعني مندوب اسمه
`Aramex` كان بيتقري «قاهرة» — ده مش تبسيط، ده **عرض قيمة غير موجودة**.

من v2.4.0 النص من الميتافيلد زي ما هو، واللون هو اللي بيصنّف:

| القيمة | اللون |
|---|---|
| `Bosta` / `بوسطة` | أحمر |
| `Showroom` / `Show Room` / `شو روم` | أخضر |
| أي مندوب تاني | أزرق |
| فاضي | «لا يوجد مندوب مسجل» — رمادي |

المقارنة بتشيل المسافات والشرطات والـ underscore وبتقارن lowercase، فـ
`Show_Room` و`show room` و`Showroom` كلهم أخضر.

### عمود «خيارات» — لينكات خارجية بس

زرارين `<a target="_blank" rel="noopener">` في `RDY_OPT_LINKS`:

```
عطلان  → https://ecommoda-dev.github.io/Order-Item-Remover/
طباعة  → https://ecommoda-dev.github.io/Order-Printer/
```

> ⚠️ **مفيش أي أكشن على الأوردر ومفيش نداء للـ Worker** — دول بيفتحوا أدوات
> تانية وبس (قرار أحمد صريح). لو حد ضاف أكشن هناك بعدين، لازم يعدّي على
> حارس التغليف زي أي كتابة تانية.

### التحديث التلقائي كل ١٥ دقيقة — `§READY-JS::autoRefresh`

> ⚠️ **ده النداء الدوري الوحيد في الأداة، وقرار مقصود** (أحمد 05-09-2026)
> بينقض السطر اللي كان مكتوب هنا «مفيش polling». الفترة **١٥ دقيقة** —
> `get_ready_orders` بيصفّح كل الأوردرات ويصنّف S2 لكل واحد، فأي فترة أقصر
> تكلفتها حقيقية على الـ Worker وعلى حدود شوبيفاي.

- `rdyAutoRefreshTick` **مابيستعلمش والطابور مش معروض** (`rdyQueueVisible`:
  تاب «فحص أوردر» نشطة **و**`#subscreenEntry` ظاهرة). في الحالة دي بيعلّم
  `rdyDirty` بس، والجلب بيحصل أول ما الموظف يرجع — فمافيش استعلام على قايمة
  محدش شايفها، ومافيش إعادة رسم تحت إيد موظف بيغلّف.
- `rdyArmAutoRefresh` بتتنادى بعد الدخول **وبعد كل جلب ناجح**، فالعدّاد معناه
  «١٥ دقيقة من آخر تحديث فعلي» مش من الدخول. والتسليح عند الدخول مقصود:
  لو أول جلب فشل، تفضل فيه محاولة تلقائية بدل ما الطابور يقف للأبد.
- بتقف في `rdyResetAll` (الخروج)، وأي نبضة بتلاقي `currentEmployee = null`
  بتوقف نفسها.

### الشكل — الهيدر اتشال والتصدير اتشال

- **عنوان القسم والسطر التوضيحي اتشالوا**؛ لوحة «آخر تحديث» وزرار التحديث
  بقوا **جوّه صندوق الأعداد** (`.rdy-actions`).
- **صندوق الأعداد بقى ظاهر في كل الحالات** — لأنه شايل زرار التحديث. لو
  اتخفى في «في انتظار التحديث» أو «لا توجد أوردرات» يبقى مفيش طريقة تحدّث
  بيها. والأعداد بتبقى **«—» مش «0»** قبل أول جلب ناجح: «ما اتحدّثش» مش
  «مفيش شغل» (`rdySetCounts(null,…)`).
- **زرار تصدير XLSX اتشال** من الطابور ومعاه `rdyExportXLSX` كلها. التصدير
  لسه موجود في **تاب السجل** — لو رجع للطابور تاني، لازم يرجع من نفس مصدر
  الفلترة (`rdyFilteredRows`) بترتيب الأعمدة الحالي.

### العمودان المحسوبان — نفس درجات اللون

`rdyDayLevel(days)` هي **المصدر الوحيد** للدرجة (`tb-d0` … `tb-d7`)، و«الوقت
منذ الطباعة» و«عمر الأوردر» بياخدوا منها الاتنين. النص هو اللي بيفرق بينهم
مش اللون. و«من امبارح» بقت **«منذ أمس»** في العمودين.

## المدخل التالت — ماسح باركود بوسطة (v2.5.0 · 05-09-2026)

> ⚠️ **النسخة دي فيها تعديل على الـ Worker** (`§BOSTA` + `get_order?tracking=`)
> — يعني **Promote مطلوب**، و**سر جديد** `BOSTA_API_KEY` لازم يتضاف على
> الـ Worker قبل النشر. `MIN_WORKER_VERSION` بقى `2.4.0`.

### المشكلة اللي بيحلّها

الموظف في المخزن قدامه أحيانًا ملصق بوسطة مش باركود الأوردر. لحد v2.4.0 كان
لازم يفتح أداة تانية يطلّع منها رقم الأوردر، وبعدين يرجع يكتبه بإيده هنا.
الطريقة دي مستخدمة أصلاً في `Bosta-Orders-Shipped-Scanner` — واللي اتنقل هنا
هو **جزء القراءة منها بس**.

### إزاي بيشتغل — ونقطة الاتحاد المهمة

```
تراكينج بوسطة → /deliveries/search → businessReference → cleanOrderName
              → نفس مسار `name` بالحرف: البحث بالاسم → fetchOrderForPack
              → analyzeStage → classifyOrderItems → evaluatePackGuard
```

> ⚠️ **مفيش مسار تغليف تاني، ومينفعش يتعمل.** التحويل بيحصل في أول
> `get_order` وبعده الكود واحد للمدخلين. مسار موازي معناه **حارسين**
> لـ«اتغلّف قبل كده» يفترقوا مع الوقت — وده بالظبط اللي أنتج R3 (راجع
> القسم اللي تحت).

### الأداة **مابتكتبش** على بوسطة

النداء الوحيد `POST /deliveries/search` — قراءة بحتة. مفيش أي تغيير لحالة
شحنة ومفيش صف D1 جديد (الفعل المسجَّل لسه هو `packed` زي ما هو). أي «تحديث
حالة على بوسطة» **مش** شغل الأداة دي — ده شغل `Bosta-Orders-Shipped-Scanner`.

### ⚠️ الخطر المفتوح — §BOSTA بقى في مكانين

```
Orders-Packing-Checker/index.js        §BOSTA::bostaSearch · cleanOrderName
Bosta-Orders-Shipped-Scanner/index.js  §BOSTA::bostaSearch · cleanOrderName
```

**نفس عيلة خطر `classifyS2Subtype`** المكتوب فوق. أي إصلاح على:

- شكل رد بوسطة (`extractDeliveries` بأشكاله الأربعة)
- `cleanOrderName` وفخّ الـ `#` في `businessReference`
- قاعدة «الفشل بيرمي مايرجّعش قايمة فاضية» (مفتاح غلط ≠ شحنة غير موجودة)

لازم يتعمل في **الريبوهين**. الاختلاف الوحيد المقبول: الأداة دي مابتستخدمش
`BOSTA_STATE_MAP` في أي **قرار** — عرض بس؛ الانتقال المسموح بيتحدّد من
شوبيفاي (`analyzeStage`) زي أي سكان تاني.

### رسائل الفشل — تلات حالات مختلفة

| الحالة | الرد | الرسالة |
|---|---|---|
| `BOSTA_API_KEY` ناقص | `500` من `assertEnv` | **باسم المتغيّر** — مش «الشحنة غير موجودة» |
| بوسطة ردّت ومالقتش الرقم | `404` + `step:'bosta'` | «رقم التتبع X مش موجود على بوسطة» |
| بوسطة لقتها بس بلا `businessReference` | `404` + `step:'bosta'` | «شحنة X موجودة على بوسطة بس من غير رقم أوردر» |
| الأوردر مش على شوبيفاي | `404` + `step:'shopify'` | بيقول **التراكينج ورقم الأوردر** الاتنين — الموظف يعرف مين الناقص |

> ⚠️ **مفتاح غلط ≠ شحنة غير موجودة.** `bostaSearch` بترمي على أي `!res.ok`
> بدل ما ترجّع قايمة فاضية — لأن «مالقيناهاش» بتخلّي الموظف يسيب شحنة
> موجودة فعلاً. القاعدة دي منقولة مع الكود، **ممنوع تبسيطها**.

### شريط المصدر فوق شاشة التشييك

`order.bosta` بيرجع من الـ **Worker** (مش من الواجهة) وفيه التراكينج + نوع
بوسطة + حالتها، والواجهة بتعرضهم في شريط أحمر فوق الشاشة عشان الموظف يقارن
بالملصق. بيتصفّر في **كل** استعلام قبل ما يتملّى وفي `resetToEntry` — من غير
كده أوردر اتفتح من سكانر شوبيفاي هيفضل شايل تراكينج الأوردر اللي قبله.

## شكل مربعات الإدخال والفوكس — v2.5.0

### التلاتة جنب بعض بالتساوي (قرار أحمد، 05-09-2026)

```
[ ماسح باركود شوبيفاي ]  [ ماسح باركود بوسطة ]  [ إدخال يدوي ]
        أزرق                     أحمر               محايد
```

`.entry-grid` = `repeat(3, 1fr)` — مقاس واحد للتلاتة مقصود: مفيش مدخل
«رئيسي» وتاني «ثانوي»، الموظف بيختار حسب الملصق اللي في إيده.

> ⚠️ **اللون هو التمييز الوحيد بينهم** — أي توحيد للألوان بيرجّع مشكلة
> «سكنت في المربع الغلط»، واللي تكلفتها استعلام فاشل ووقت ضايع في نقطة شغل.

### `lastEntryMode` — الفوكس بيرجع لآخر مربع اتشتغل منه

| اللحظة | النتيجة |
|---|---|
| سكان/بحث من أي مربع | `lastEntryMode` بياخد المربع ده |
| الفوكس راح لمربع تاني (ماوس/تاب) | `lastEntryMode` بياخده كمان |
| رجوع من شاشة التشييك (`resetToEntry`) | الفوكس بيرجع **لنفس المربع** |
| إلغاء نافذة «اتغلّف قبل كده» | نفس المربع |
| خروج (`doLogout`) | بيرجع للافتراضي (`shopify`) |

> ⚠️ **في ذاكرة الصفحة بس — مش `localStorage`.** المفتاح الوحيد في التخزين
> هو السر (Standards #28)، وموظف جديد بيدخل على نفس الجهاز المفروض يبدأ من
> المربع الافتراضي مش من مربع زميله.

## الطابور — تعديلات v2.5.0

### مربعات المناطق — فلتر بضغطة

> ⚠️ **القسم ده بقى تاريخي من v2.6.2** — مربعات المنطقة اتبدّلت بمربعَي
> «بوسطة/مناديب» على عمود المندوب، وعمود المنطقة نفسه اتشال من الجدول
> نهائيًا. راجع قسم «v2.6.2» فوق. المبدأ («حالة واحدة مع الفلتر»، «العدّ
> من الطابور كله») اتنقل زي ما هو للمربعين الجداد.

في المساحة الفاضية في نص صندوق الأعداد: مربع لكل منطقة موجودة في الطابور
+ عددها، **بنفس ألوان عمود المنطقة** (نفس قواعد الـ CSS، مش نسخة منها).

- **العدّ من `rdyAllOrders` (الطابور كله) مش من المفلتر.** لو اتحسب على
  المفلتر، أول ضغطة كانت هتصفّر باقي المربعات ومحدش يقدر يرجّع.
- **الضغط بينادي `rdyMsToggleItem('zone', …)` نفسها** — يعني المربعات وفلتر
  «المنطقة» **حالة واحدة**، مش حالتين متوازيتين. حالتين معناهم مربع مولّع
  وفلتر فاضي (أو العكس)، والموظف مش هيفهم ليه الجدول ناقص.
- الترتيب **ثابت** (ترتيب الـ choice list ثم الغريب أبجديًا) — الترتيب
  بالعدد كان هيرقّص المربعات مكانها مع كل تحديث.
- القيمة بتتقرا من `dataset.zone` مش من نص متسرّب جوّه `onclick` — منطقة
  برّه القايمة بتتعرض زي ما هي، وأي علامة تنصيص فيها كانت هتكسر النداء.

### باقي التعديلات

| قبل | بعد |
|---|---|
| فلاتر: النوع · المندوب · المحاولة | **المنطقة** · النوع · المندوب · المحاولة |
| قسم الفلاتر مفتوح افتراضيًا | **مقفول افتراضيًا** (`flt-body collapsed` + `rdyFltOpen = false`) |
| نوع الأوردر بادج | **نص عادي** بنفس الألوان (`.type-text`) |
| المندوب الفاضي: «لا يوجد مندوب مسجل» | **«غير محدد»** — نفس مفردة عمود المحاولة |
| زرار «طباعة» | **«إعادة 🖨»** — نفس الرابط، لسه لينك خارجي بلا أكشن |

> ⚠️ `rdyFltOpen = false` لازم يفضل **متطابق** مع `class="flt-body collapsed"`
> في الـ HTML، وإلا أول ضغطة على هيدر الفلاتر مابتعملش حاجة ظاهرة.

> ⚠️ قيمة فلتر المنطقة هي **النص المترجم المعروض** (`o.zone`) مش قيمة
> الميتافيلد — نفس اللي بيتفلتر بيه في `rdyFilteredRows` وبيتعرض في الجدول.

## v2.6.0 (05-09-2026) — واجهة بس · مفيش Promote

> **`index.js` ما اتلمسش** — التلاتة تحت محسوبين في المتصفح.
> `MIN_WORKER_VERSION` زي ما هو (`2.4.0`).

| قبل | بعد |
|---|---|
| رقم الأوردر في شاشة التغليف **نص عادي** | **لينك لشوبيفاي** (`<a id="checkOrderName">`) |
| عمود «المحاولة» **بادج** | **نص عادي** بنفس الألوان (`.attempt-text`) |
| زرار «إعادة 🖨» بإيموجي | **أيقونة SVG** (`RDY_PRINT_ICON`) |

### رقم الأوردر في شاشة التغليف — لينك (قاعدة #20)

العنصر بقى `<a class="order-name" id="checkOrderName" target="_blank" rel="noopener">`،
و`renderCheckScreen` بتحط الـ `href` من `currentOrder.id` — نفس شكل الرابط اللي
في `orderLink()` وفي `renderConflictPanel`.

> ⚠️ **الأوردر اللي جه من غير `id` بيتعرض نص عادي** (`.no-link`) مش لينك بلا
> هدف: لينك بيروح لـ `orders/undefined` أسوأ من نص، لأن الموظف بيفتحه ويلاقي
> صفحة خطأ ويفتكر الأوردر اتمسح. (`currentOrder.id` بييجي `legacyResourceId`
> رقمي من الـ Worker — مش `gid`.)

### عمود «المحاولة» — نص بلا بادج

`.attempt-badge` اتشالت وبقت `.attempt-text` بنفس ألوان البادج القديم:
محاولة مكرّرة **كهرماني** · أول شحنة **رمادي** · `na` شرطة زي ما هي.
**نفس اللي اتعمل في «نوع الأوردر» في v2.5.0** — اللون هو اللي فضل شايل
التصنيف بعد ما الخلفية اتشالت، فمينفعش يتوحّد مع باقي نص الجدول.

### أيقونة الطابعة — SVG مش إيموجي

الإيموجي `🖨` (U+1F5A8) **مش مدعوم على أجهزة كتير** (ومنها أجهزة المخزن) —
كان بيطلع مربع فاضي، يعني زرار بلا أيقونة. بقى `RDY_PRINT_ICON`: SVG inline
بـ `stroke="currentColor"` فبيورث لون الزرار (أبيض على الكهرماني) و`.opt-ico`
بتديله مقاس ثابت ١٤px.

> ⚠️ **ممنوع الرجوع لإيموجي هنا.** الأيقونة اللي مابتترسمش مابتفشلش بصوت —
> بتبان زرار ناقص، والموظف مش هيبلّغ عنها.

## v2.6.1 (05-09-2026) — واجهة بس · مفيش Promote

> **`index.js` ما اتلمسش.** `MIN_WORKER_VERSION` زي ما هو (`2.4.0`).

### زرار «إغلاق» في نافذة «تم تغليف هذا الأوردر مسبقاً» — بقى أحمر

كان `.btn-ghost` (رمادي محايد) زي أي زرار إغلاق عادي. النافذة دي بترفض
تكرار التغليف (409 + `conflict: true`)، مش مجرد معلومة — فالزرار بقى
`.btn-red` عشان يعبّر عن الرفض بصريًا مش يتوه وسط أزرار الإلغاء العادية.

كلاس جديد على نفس نمط `.btn-green` الموجود — بتوكنز `:root` الموجودة
بالفعل، **صفر hex جديد**:

```css
.btn-red{padding:9px 18px;border-radius:var(--radius-sm);border:none;background:var(--red);color:var(--on-accent);font-family:var(--font-body);font-size:13px;font-weight:700;cursor:pointer;transition:background .15s}
.btn-red:hover{background:var(--red-dark)}
```

`--red` · `--red-dark` · `--on-accent` كلهم معرّفين في `:root` من قبل
(مستخدمين في `.modal-close-x` و`.pin-key.utility`). `.btn-red` بقى الشكل
المعتمد لأي زرار «إغلاق/إلغاء» رئيسي في نافذة بتوصف حالة سلبية أو نهائية —
مش بديل لـ `.modal-close-x` (زرار الـ ✕ الصغير في هيدر أي modal، قاعدته
منفصلة).

## v2.6.2 (05-09-2026) — واجهة بس · مفيش Promote

> **`index.js` ما اتلمسش.** `MIN_WORKER_VERSION` زي ما هو (`2.4.0`). الـ
> Worker لسه بيرجّع `zone` (`custom.zone`) في `get_ready_orders` زي ما هو —
> الواجهة بس هي اللي بقت متجاهلاه تمامًا (قرار أحمد 05-09-2026).

### عمود «المنطقة» اتشال نهائيًا

مش موقوف مؤقتًا — **محذوف**: مفيش `<th>` ولا `<td>` للمنطقة في جدول الطابور،
ومفيش فلتر «المنطقة»، ومفيش `rdyMsState.zone`. `colspan` صف «لا توجد نتائج»
نزل من `10` لـ `9`. راجع «`custom.zone` — المنطقة» تحت قسم v2.4.0 فوق —
المحتوى هناك بقى **تاريخي** (بيوصف اللي كان موجود، مش اللي موجود دلوقتي).

### مربعات الفلتر فوق الجدول بقت على المندوب مش المنطقة

بدّلت مربعات المنطقة الأربعة (قاهرة وجيزة/محافظات/شو روم/تحذير) مربعان
بس، على **عمود المندوب**:

| المندوب | المربع | اللون |
|---|---|---|
| `Bosta` (نفس تصنيف `.courier-bosta`) | «بوسطة» | أحمر (نفس `--red-*`، زي ما كانت «محافظات») |
| أي حاجة تانية — **حتى شو روم وحتى لا يوجد مندوب مسجل** | «مناديب» | أزرق (نفس `--accent-*`، زي ما كانت «قاهرة وجيزة») |

> ⚠️ الشو روم أخضر في عمود المندوب نفسه، بس هنا بيقع جوّه «مناديب» —
> المربعين ثنائيين بس (بوسطة/غير بوسطة)، مش نسخة من الأربع تصنيفات
> الموجودة في `rdyCourierClass`.

### حالة واحدة مع فلتر «المندوب» — نفس مبدأ v2.5.0

فلتر «المندوب» التفصيلي (اختيار متعدد بأسماء المناديب الحقيقية) بقى **أول
فلتر** في القسم بدل «المنطقة» — نفس السبب اللي خلّى المنطقة أول فلتر في
v2.5.0: المربعين فوق بيكتبوا فيه. الضغط على مربع بينادي
`rdyCourierGroupValues(group)` (بتجيب كل قيم المندوب الحقيقية اللي بتقع في
المجموعة دي من `rdyMsState.courier.items`)، وبيحدد/يلغي **كلها** جوّه
`rdyMsState.courier.selected` — **مش** حالة فلترة موازية. حالتين معناهم
مربع مولّع وفلتر فاضي، أو العكس، نفس الفخ اللي كان موثّق لمربعات المنطقة.

> ⚠️ **أثر جانبي مقبول:** لو مندوب جديد (مش بوسطة) ظهر في الطابور بعد
> تحديث وفلتر «مناديب» كان مفعّل، القيمة الجديدة **مش** بتتضاف تلقائيًا
> لحد ما الموظف يدوس تاني على المربع — نفس سلوك «تحديد الكل» الموجود
> بالفعل في باقي الفلاتر (تحديد جزئي = مش محدد بصريًا).

## حارس التغليف المزدوج — server-side من v2.1.0 (R3)

قبل v2.1.0 حارس «اتغلّف قبل كده» كان في `get_order` **فقط**، و`complete_pack`
كان بياخد `stage` و`items` من الطلب وبيكتب على طول. يعني الحارس كله كان في
العميل — و«التحقق في العميل بس مش تحقق» (`ecommoda-order-lifecycle` §1.5).

**ده مش خطر نظري — حصل فعلاً.** استعلام على D1 (03-09-2026) لقى تلات أوردرات
اتغلّفوا **مرتين في نفس المرحلة خلال دقايق**:

| الأوردر | الفرق | المرحلة |
|---|---|---|
| `#43277` | ١٠٠ ثانية | (قبل عمود stage) |
| `#45227` | ١٦٧ ثانية | S1 → S1 |
| `#47507` | ٤٣٣ ثانية | S1 → S1 |

دلوقتي `complete_pack` بيعمل الآتي **قبل أي كتابة**:

1. يقرا الأوردر من شوبيفاي (`fetchOrderForPack`)
2. يحسب `stage` بـ `analyzeStage` — **أي `stage` جاي من العميل بيتجاهل تمامًا**
3. يصنّف البنود بـ `classifyOrderItems` — **بنود العميل مش مصدر حقيقة**
4. ينادي `evaluatePackGuard` — نفس الدالة اللي `get_order` بينادها

**عقد الردود الجديد:**

| الحالة | الرد |
|---|---|
| اتغلّف والبنود ما اتغيّرتش | `409` + `conflict: true` — **مفيش أي كتابة** |
| اتغلّف والبنود اتغيّرت وبدون `editReason` | `409` + `conflict: true` + `requiresReason: true` |
| مفيش بنود للمرحلة دي | `409` برسالة صريحة |
| بنود العميل ≠ بنود السيرفر | بيكمّل **ببنود السيرفر** + تحذير «الأوردر اتعدّل بين ما فتحته وما ضغطت تم» |
| تعارض/التباس في `analyzeStage` | بيكمّل + تحذير بيتسجّل مع الصف |

> ⚠️ **`§PACK::evaluatePackGuard` هي المصدر الوحيد لمنطق الكشف.** أي تعديل
> على «اتغلّف قبل كده» مكانه هناك لوحده — الدالة اتفصلت عشان `get_order`
> و`complete_pack` **يستحيل** يفترقوا. تكرار المنطق في المكانين هو بالظبط
> اللي أنتج R3.

> ⚠️ **البصمة المخزّنة بقت من بنود السيرفر مش بنود العميل.** لازم تعبّر عن
> حالة الأوردر **وقت الكتابة** لأن `get_order` بيقارن بيها المرة الجاية —
> بنود عميل قديمة كانت هتخزّن بصمة قديمة وتدي كشف تغيير كاذب بعدين.

**التكلفة:** استعلام قراءة إضافي واحد لكل `complete_pack`. مقبول تمامًا
مقابل منع التغليف المزدوج.

## فخاخ الأداة دي

- **الـ `UPDATE` بعد `writeLog` بيطابق بالـ `timestamp` بالظبط.** لو حد غيّر
  `writeLog` بحيث يحط timestamp من عنده بدل الممرَّر، المطابقة بتفشل. الفشل ده
  **بقى مسموع** من v2.0.0 (`columnsWritten:false` + `warning`)، وكشف التغيير
  محميّ منه لأنه بيقرا `extra.fingerprint` — بس عرض السجل هيبان ناقص.
- **صيغة `items` (`SKU ×qty` مفصولة بفاصلة) لسه مشتركة** بين `complete_pack`
  و`parseSavedItemsString` في `get_order` — **الملفّين لازم يتغيّروا مع بعض**.
  دلوقتي هي **fallback للصفوف القديمة بس**؛ الصفوف الجديدة بتتقارن بـ
  `extra.fingerprint`. ولو التحليل النصي فشل (منتج من غير SKU واسمه فيه فاصلة)
  النتيجة بقت "معرفناش" → `changeDetected = true` (الافتراضي الآمن).
- **`fulfillableQuantity` هو مصدر الحقيقة الوحيد** لتصنيف المنتجات من v1.5.0.
  متضفش فلترة يدوية على الـ refunds تاني. ومن v2.0.0 قطعة الاستبدال اللي ليها
  نظير في `lineItems` بتاخد كميتها من نفس المصدر — قبل كده الكمية كانت بتختلف
  حسب أنهي حلقة سبقت، فالبصمة كانت بتتغيّر من غير تغيير حقيقي.
- **الاتصالات مصفّحة، والقصّ بيتبلّغ.** `lineItems` و`returns` بيتصفّحوا لحد
  10 صفحات؛ أي بقايا (أو `exchangeLineItems` أكتر من 50) بترجع `truncated:true`
  والواجهة بتعرض بانر. **ممنوع** الرجوع لـ `first: 50` بلا تصفيح — البصمة
  بتتحسب على القايمة، فالقصّ الصامت = كشف تغيير غلط.
- **حقول مهجورة في الاستعلام (متحقَّق منها على schema 2026-01 يوم 03-09-2026):**
  `LineItem.fulfillableQuantity` · `ProductVariant.image` · `Product.featuredImage`
  كلهم `deprecated` بس شغّالين. **متستبدلهمش كتنضيف** — البدائل
  (`FulfillmentOrderLineItem.remainingQuantity` · `media` · `featuredMedia`) بتغيّر
  السلوك والدلالة، و`fulfillableQuantity` تحديدًا هو مصدر الحقيقة المعتمد للأداة.
- **الصلاحيات المطلوبة فعليًا:** `read_orders` · `write_orders` · `read_returns` ·
  `read_products` — و`?action=diag` بيفحصهم على `currentAppInstallation`.
- **§BOSTA في مكانين** — هنا وفي `Bosta-Orders-Shipped-Scanner`. أي إصلاح
  على شكل رد بوسطة أو على `cleanOrderName` لازم يتعمل في الاتنين. نفس عيلة
  خطر `classifyS2Subtype` المكتوب فوق.
- **`cleanOrderName` مش تنضيف تجميلي** — `businessReference` بتيجي من بوسطة
  بالـ `#`، ومن غيرها كل أوردر بيترفض بـ«غير موجود على شوبيفاي» (باج v2.0.0
  في الأداة التانية).
- **مدخل بوسطة مالوش مسار تغليف خاص بيه** — التحويل بيحصل في أول `get_order`
  وبعده الكود واحد. مسار موازي = حارسين لـ«اتغلّف قبل كده» يفترقوا = R3 تاني.
- **`BOSTA_API_KEY` غايب بيوقف مدخل بوسطة بس** — باقي الأداة شغّالة عادي،
  والرسالة بتبقى **باسم المتغيّر** مش «الشحنة غير موجودة».
- **مربعا «بوسطة/مناديب» وفلتر «المندوب» حالة واحدة (v2.6.2)** — الضغط
  بيحدد/يلغي كل قيم المندوب الحقيقية اللي بتقع في المجموعة دي جوّه
  `rdyMsState.courier.selected`. أي حالة موازية معناها مربع مولّع وفلتر
  فاضي، والموظف مش هيفهم ليه الجدول ناقص. (بدّلت مربعات المنطقة اللي
  كانت بتنادي `rdyMsToggleItem('zone', …)` — العمود ده اتشال نهائيًا.)
- **عدّ المربعين من الطابور كله مش من المفلتر** — لو اتحسب على المفلتر،
  أول ضغطة تصفّر باقي المربع ومحدش يرجّع.
- **أيقونة زرار «إعادة» SVG مش إيموجي** — `🖨` (U+1F5A8) مابيترسمش على أجهزة
  كتير وبيطلع مربع فاضي. أي رجوع لإيموجي = زرار بلا أيقونة، والفشل ده صامت.
- **عمود «المحاولة» نص بلا بادج** — نفس قاعدة «نوع الأوردر»: اللون هو التصنيف
  بعد ما الخلفية اتشالت، فمينفعش يتوحّد مع باقي نص الجدول.
- **رقم الأوردر في شاشة التغليف لينك، وبيرجع نص عادي لو مفيش `id`** — لينك
  رايح لـ `orders/undefined` بيخلّي الموظف يفتكر الأوردر اتمسح.
- **`rdyFltOpen` لازم يطابق `collapsed` في الـ HTML** — لو اتفكّوا، أول ضغطة
  على هيدر الفلاتر مابتعملش حاجة ظاهرة.
- **`lastEntryMode` مش في `localStorage`** — لو حد خزّنه، موظف جديد على نفس
  الجهاز هيبدأ من مربع زميله. والمفتاح الوحيد في التخزين هو السر (#28).
- **شريط مصدر بوسطة بيتصفّر في كل استعلام** — من غير التصفير، أوردر اتفتح من
  سكانر شوبيفاي بيفضل شايل تراكينج الأوردر اللي قبله والموظف يقارن بالغلط.
- **الأداة بتقرا `custom.manual_status` و`custom.status_2_r_e`** — أي تعديل على
  نصوص الحالات مكانه `ecommoda-order-lifecycle`، والقيم هنا (`S1_VALUES` ·
  `S2_VALUES`) لازم تتطابق معاها. **`'Printed'` اتشالت في v2.0.0** — مش موجودة
  في أي من الـ choice lists.
- **`ENV_REQUIRED` فيها `shopify` بس** — مجموعتَي `bosta` و`stock` اتشالوا في
  v2.1.0 (R11). كانوا بقايا قالب: الأداة مش بتنادي بوسطة ومش أداة مخزون،
  والمتغيّرين مش في `wrangler.toml` أصلاً. `assertEnv(env,'stock')` بالسهو
  كان هيفشل برسالة مضلّلة عن متغيّر مالوش لازمة.
- **fallback الـ CORS في `json()` بقى `ALLOWED_ORIGINS[0]`** مش `'*'` (R13).
  الفرع ده كود ميّت (كل النداءات بتمرّر `request`)، بس التناقض كان بيخلّي أي
  استدعاء مستقبلي من غير `request` يفتح wildcard في **أداة كتابة**.
- **الترتيب في تاب السجل بيشتغل على صفوف الصفحة المحمّلة بس** — الفلترة
  والتصفيح server-side. ده مقصود وموثّق في مودال الـ About.
- **منطق الـ multi-select والترتيب متكرر في الواجهة** بين `§LOG-JS`
  (فلترة **server-side** + صفحات) و`§READY-JS` (فلترة **client-side** على
  قايمة محمّلة كاملة). الفصل **مقصود** — الاتنين شغّالين في نفس الصفحة في
  نفس الوقت، ومشاركة `msState` أو `sortState` أو `dateFrom` بينهم معناها إن
  فلتر الطابور يروح للسيرفر مع نداء السجل والعكس. **بس الثمن حقيقي:** أي
  تعديل على سلوك القوايم أو الترتيب لازم يتعمل في **الاتنين**. لو التكرار
  ده كبر، الحل تعميم الدوال على registry — **مش** نسخة تالتة.
- **`READY_MAX_PAGES` (20) مش `MAX_PAGES` (10).** الأول صفحات **قايمة
  أوردرات** (250 للصفحة) في الطابور، والتاني صفحات **بنود أوردر واحد** في
  التغليف. الاسمين اتفصلوا في v2.2.0 عمدًا — رقمين بمعنيين مختلفين تمامًا.
- **«الوقت منذ الطباعة» و«عمر الأوردر» بيتحسبوا بفرق الأيام التقويمية**
  بتوقيت القاهرة، مش بالساعات. أي «تبسيط» لـ `(Date.now() - t)/86400000`
  بيكسر الدلالة: أوردر اتطبع ١١ مساءً هيفضل «نفس اليوم» لحد ١١ مساء
  اليوم اللي بعده.
- **الأرقام النسبية بتتحدّث في مكانها مش بإعادة رسم الجدول** — لو حد بدّل
  `rdyTick` بـ `rdyApplyFilters()`، تمرير الجدول هيتصفّر كل نص دقيقة.
- **النبضة مالهاش أي علاقة بالشبكة.** مفيش polling على `get_ready_orders`
  ومايتضافش من غير قرار — الـ endpoint بيصفّح كل الأوردرات ويصنّف S2
  لكل واحد.
- **المندوب مابيتحوّلش.** أي رجوع لـ «غير Bosta = قاهرة» بيرجّع باج عرض
  قيمة غير موجودة. اللون بس هو التصنيف.
- **عمود «المنطقة» اتشال نهائيًا من v2.6.2** — مفيش `custom.zone` في الجدول
  ولا في الفلاتر خالص. الـ Worker لسه بيرجّعه في `get_ready_orders`، بس
  الواجهة بقت متجاهلاه. أي كود بيدوّر على `rdyZoneInfo`/`RDY_ZONE_MAP`/
  `rdyMsState.zone` هيلاقيه اتشال، مش مجرد مخفي.
- **مربعا فلتر المندوب ثنائيين بس (بوسطة/مناديب)** — الشو روم أخضر في
  عمود المندوب نفسه، بس بيقع جوّه «مناديب» الأزرق في المربعين. مربع تالت
  لشو روم مش قرار اتاخد.
- **فيه polling دلوقتي — كل ١٥ دقيقة** (v2.4.0). أي تقصير للفترة قرار له
  تكلفة: الـ endpoint بيصفّح كل الأوردرات ويصنّف S2 لكل واحد. والنبضة
  مابتستعلمش والطابور مخفي — بتعلّم `rdyDirty` بس.
- **صندوق الأعداد مابيختفيش** — هو شايل زرار التحديث. لو رجع يختفي في
  حالات «في انتظار التحديث» و«لا توجد أوردرات»، الموظف مايبقاش قادر يحدّث.
- **الطابور مابيكتبش أي حاجة** — لا على شوبيفاي ولا في D1. `get_ready_orders`
  قراءة بحتة، فمفيش `type` جديد في `logs` ومفيش تسجيل مطلوب في
  `ecommoda-constants` §7.
- **فشل الطابور مابيتحوّلش لقايمة فاضية.** `shopifyGQL` بترمي، والفشل بيظهر
  كتوست + رجوع لحالة «في انتظار التحديث» — مش «لا توجد أوردرات معلّقة».
  «تعذّر الاستعلام» ≠ «مفيش شغل»، والبانرين فوق الجدول بيفرّقوا بين
  «معرفناش نصنّف» (`s2Failed`) و«صنّفنا على بيانات مقصوصة» (`s2Truncated`).

## استرجاع النسخ القديمة

> ده بديل الـ tags — دفع الـ tags ممنوع من جلسات Claude Code السحابية.

```
النسخ المرقّمة الخمسة (3.html · 4.html · 5.html · Index-v1.1.2.html · Index-v1.1.3.html)
محفوظة كلها في commit: de523364b8d06f42c4ba8984a0b711603b308dcd

git show de52336:3.html
git show de52336:Index-v1.1.3.html
```

`5.html` (نسخة الواجهة قبل النقل) محفوظة كمان في آخر commit قبل النقل:
`d2a5dd8b22a180e7be3eee951e35263677b54787`.

نسخة الواجهة والـ Worker قبل مراجعة v2.0.0 محفوظة في: `f422be1`.

## بصمة المهارات

| المهارة | الإصدار وقت آخر تعديل |
|---|---|
| ecommoda-worker-builder | v2.0.0 |
| ecommoda-html-builder | v6.3.0 |
| ecommoda-constants | v1.4.4 |
| ecommoda-order-lifecycle | v1.2.0 |
| shopify-graphql-helper | v1.0.0 |
| bosta-api-helper | — (خارج نظام الإصدارات) |

آخر مطابقة: 05-09-2026 · `index.js` v2.4.0 · `index.html` v2.6.2

> ✅ **فحص Step 9 الآلي اتنفّذ على الملف النهائي** (05-09-2026) — كل البنود
> عدّت: سلّم z-index (login 500 · modals 600 · toast 9999 · محتوى ≤ 210) ·
> صفر `#hex` حرفي خارج `:root` · صفر متغيّر وهمي · حارس
> `document.contains(e.target)` موجود للقايمتين · `[...rows].sort()` مش
> in-place · `--container-max` على Tier L.
>
> ✅ **واختبار متصفح فعلي** (Playwright + Worker مموّه): الدخول · تحميل
> الطابور · العدّادات (3/2/1/1) · البانرين مع بعض · «المندوب الفاضي = غير
> محدد» · الجدول **مابيفتحش التشييك بالضغط** · القائمة **بتفضل مفتوحة بعد
> الاختيار** (بند ٣٢) · الترتيب بتلات حالات والأيقونة بتختفي في التالتة ·
> فصل `msState` عن `rdyMsState` · التنقل بين التابات مابيضيّعش الطابور ·
> `rdyResetAll()` عند الخروج · **وحارس النسخة في الاتجاهين** (Worker 2.1.0
> بيولّع التحذير · 2.2.0 و3.0.0 ساكتين).
>
> ✅ **v2.3.0 — اختبار متصفح فعلي كمان** (Playwright + Worker مموّه): ترتيب
> الأعمدة الجديد · سلّم التأخير بدرجاته الخمسة على أوردرات بأعمار مختلفة ·
> عدّاد عمر الأوردر · الترتيب الافتراضي (الأقدم أولاً) والضغطة التالتة
> بترجّع له · `sincePrint` باتجاهه المعكوس · بادج الزرار · سلّم قِدَم
> «آخر تحديث» (٠ · ٣ · ٩ · ٢٢ · ٧٥ دقيقة) · النبضة بتحدّث في مكانها من
> غير ما تلمس عدد الصفوف · `rdyRefreshIfDirty` بينادي الجلب **مرة واحدة**
> عند الرجوع ومن تاب السجل ومابينادهوش لو مفيش تغليف · أوردر من غير وقت
> طباعة بيدي «— لا يوجد وقت طباعة» مش خانة فاضية · وتصدير XLSX
> **اتجرّب ببديل مموّه لـ ExcelJS** — الرؤوس والصفوف مطابقة للجدول.
>
> ✅ **v2.4.0 — اختبار متصفح فعلي** (Playwright + Worker مموّه): ترتيب
> الأعمدة العشرة · بادجات المنطقة الأربعة **وقيمة برّه القايمة** (بتتعرض
> زي ما هي) **والغايبة** (تحذير) · ألوان المندوب الأربعة على
> `Bosta`/`bosta`/`Showroom`/`Aramex`/`أحمد`/فاضي · زراير «خيارات»
> (الروابط الصح · `target="_blank"` · مفيش أكشن) · بادج العمر بنفس درجات
> بادج الطباعة · «منذ أمس» · صندوق الأعداد ظاهر في كل الحالات و«—» قبل
> أول تحديث · التصدير **مش موجود** في الطابور · والتحديث التلقائي:
> بيستعلم والطابور ظاهر، بيعلّم `rdyDirty` بس وهو مخفي، وبيوقف نفسه بعد
> الخروج.
>
> ✅ **v2.5.0 — اختبار متصفح فعلي** (Playwright + Worker مموّه): التلات
> مربعات بعرض **متساوي** (`1fr` ×٣ — اتقاس فعليًا) وبألوانهم · سكانر بوسطة
> بيفتح شاشة التشييك على الأوردر الصح وشريط المصدر بيعرض التراكينج والنوع
> والحالة · **الفوكس بيرجع لمربع بوسطة** بعد الرجوع من التشييك، وللمربع
> الأزرق بعد سكان شوبيفاي · شريط المصدر **بيتصفّر** في سكان شوبيفاي وعند
> الرجوع · تراكينج غلط = توست بالرسالة الصريحة والفوكس فاضل في مربع بوسطة
> والشاشة ما اتغيّرتش · قسم الفلاتر **مقفول** و«المنطقة» **أول فلتر** ·
> مربعات المناطق بأعدادها وألوانها (ومنطقة برّه القايمة بتتعرض زي ما هي،
> والغايبة تحذير) · الضغط على مربع **بيفلتر الجدول ويولّع نفس فلتر
> «المنطقة»** (البادج + صف الشيبس)، وضغطة تانية بترجّع الكل · نوع الأوردر
> **نص بلا بادج** (صفر `.type-badge` في الجدول) · المندوب الفاضي «غير محدد» ·
> زرار «إعادة 🖨» بنفس الرابط و`target="_blank"`.
>
> ✅ **v2.6.0 — اختبار متصفح فعلي** (Playwright · Chromium): رقم الأوردر في
> شاشة التغليف `<a>` بـ `href` مبني من `currentOrder.id` و`target="_blank"`
> ولونه لون اللينك · وأوردر من غير `id` بيرجع `.no-link` بلا `href` وبلون
> النص العادي و`cursor:default` · عمود المحاولة **صفر `.attempt-badge`** في
> الجدول والنص بألوانه (مكرّرة كهرماني · أول شحنة رمادي · `na` شرطة) ·
> وأيقونة الطابعة SVG **مرسومة فعلاً** (١٤×١٤ مقاسة في الصفحة) بلون الزرار
> الأبيض — مفيش أخطاء console.
>
> ⚠️ **اللي ما اتجربش في المتصفح (v2.3.0):** تصدير XLSX للطابور — **اتشال
> أصلاً في v2.4.0**، فالبند ده اتقفل. تصدير تاب السجل زي ما هو ولسه
> ما اتجربش من هنا (الـ CDN بتاع ExcelJS محجوب في بيئة الاختبار).
>
> ⚠️ **v2.6.1 و v2.6.2 لسه ما اتجرّبوش في متصفح فعلي** — الكود اتفحص يدويًا
> (`node -e "new Function(...)"` على كل `<script>` block للتأكد إن الـ JS
> سليم نحويًا، وعدّ الأعمدة/الـ IDs يدويًا)، بس مفيش Playwright run في
> الجلسة دي. لازم اختبار متصفح فعلي قبل ما حد يعتبرهم v2.6.0-level موثوقين.

## بعد نشر v2.5.0 — مطلوب قبل ما المخزن يستخدم مدخل بوسطة

1. **ضيف السر `BOSTA_API_KEY`** على `orders-packing-checker-worker`
   (Settings → Variables and Secrets → Secret) — **نفس قيمة** السر اللي على
   `bosta-orders-shipped-scanner`.
2. **Promote** بعد أول build، وإلا الواجهة هتطلّع «⚠️ الـ Worker نسخة قديمة»
   (`MIN_WORKER_VERSION = 2.4.0`).
3. افتح 🩺 فحص النظام — لازم بند **«مفتاح بوسطة (سكانر التتبع)»** يبقى ✅.
4. أول سكان فعلي بتراكينج حقيقي: اتأكد إن شريط المصدر الأحمر فوق شاشة
   التشييك بيعرض **نفس** رقم التتبع اللي على الملصق.

## مسائل مفتوحة

- 🔴 **`BOSTA_API_KEY` لسه ما اتضافش على الـ Worker** (وقت كتابة السطر ده) —
  مدخل بوسطة هيفشل برسالة باسم المتغيّر لحد ما يتضاف. راجع القسم فوق.
- 🟡 **أوردر متغلّف بيفضل عالق في طابور «جاهز للتغليف» لو اتطبعت فاتورته
  تاني بعد التغليف** (مثال حقيقي: `#51105` — تفاصيله والاتجاهات المطروحة
  في `docs/known-issue-reprint-stuck-in-queue.md`). السبب: الطابور
  (`isPrintedNotPacked`) بيسأل «اتطبع بعد آخر تغليف؟»، بينما حارس التغليف
  (`evaluatePackGuard`) بيسأل «البنود اتغيّرت؟» — والمعيارين مش متفقين.
  **قرار أحمد (05-09-2026): مؤجّل، بيتصلح يدويًا كل ما يحصل.** لما يتاخد
  قرار التنفيذ، راجع الملف فوق قبل أي تعديل على `isPrintedNotPacked` أو
  `evaluatePackGuard`.
- **§BOSTA في ريبوهين** — نفس وضع `classifyS2Subtype` بالظبط. الخروج منه
  مؤجّل زيّه: مكتبة مشتركة أو قفل إحدى الأداتين، والقرار ده لأحمد.
- **Build watch paths لسه `*` (الافتراضي)** — يعني أي تعديل على `index.html`
  بينشر الـ Worker تاني بنفس الكود. التضييق لـ `index.js` + `wrangler.toml`
  (`ecommoda-tool-migration-playbook` §13-ب) لسه ما اتعملش. لو اتعمل، **لازم**
  يتوثّق هنا — وأي ملف جديد يعتمد عليه الـ Worker يتضاف للقايمة، وإلا هيتجمّد بصمت.
- **الترتيب في تاب السجل client-side على الصفحة** — لو احتجنا ترتيب على كل
  النتيجة، محتاج باراميتر `orderBy` في `getLogs` وده تعديل على البلوك المشترك
  `§SHARED` (يتعمل في المهارة الأول، مش في أداة واحدة).
- **`Ready-to-Pack` لسه منشور وشغّال بالتوازي** (قرار أحمد 05-09-2026:
  «سيبه زي ما هو دلوقتي»). يعني `classifyS2Subtype` و`isPrintedNotPacked`
  في **مكانين** — وده الوضع اللي أنتج R1 بالظبط. أي إصلاح على تصنيف S2
  لازم يتعمل في الريبوهين لحد ما القديم يتقفل. راجع «الخطر المفتوح» في
  قسم الدمج فوق.

### ✅ اتقفلت في v2.1.0 (مراجعة الكود 03-09-2026)

- ~~**R3** — `complete_pack` بيثق في العميل بالكامل~~ — حارس server-side كامل
  (القسم فوق). بيقفل **بند ٨** في قرارات أحمد قبل الجهاز التاني.
- ~~**R6** — مفيش حارس لـ `WORKER_SECRET` الغايب~~ — اتضاف. كان `Bearer undefined`
  بيعدّي المصادقة لو السيكرت اتنسي أو النسخة اتنشرت بدون Promote.
- ~~**R11** — `ENV_REQUIRED` فيه مجموعات غير مستخدمة~~ — اتشالوا.
- ~~**R12** — `compatibility_date` غير موحّد~~ — اتحرّك لـ 2026-09-03 بعد فحص
  الأعلام على الكود الفعلي (القسم فوق).
- ~~**R13** — fallback الـ CORS غير موحّد~~ — بقى `ALLOWED_ORIGINS[0]`.

### ✅ اتقفلت في v2.0.0

- ~~الدومين المهجور في `ALLOWED_ORIGINS`~~ — اتشال.
- ~~`ADMIN_WORKER_URL` بيتقري من `localStorage`~~ — بقى ثابت في `§CONFIG`، ومعاه
  `WORKER_URL`؛ الاتنين اتشالوا من شاشة الإعدادات (`ecommoda-constants` §5b ·
  Standards #28). السر بس هو اللي لسه في `localStorage`.
- ~~مفيش حارس توافق في الواجهة~~ — `MIN_WORKER_VERSION` + `cmpVersion()` +
  `checkWorkerVersion()` (حد أدنى رقمي، مش تطابق حرفي) + زرار `⚠️ الـ Worker نسخة قديمة`.
- ~~مفيش زرار خروج~~ — اتضاف، وبينادي `log_logout`.

---

آخر تحديث: 05-09-2026 — 23:59

</div>
